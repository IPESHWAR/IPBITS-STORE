'use client';

/**
 * Legacy file — menu UI lives entirely in `Navbar.js` now.
 * Do not add catalog grids or standalone theme buttons here.
 */
export default function MenuDropdown() {
  return null;
}
